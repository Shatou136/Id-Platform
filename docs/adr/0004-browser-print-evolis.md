# Print from the browser to the Evolis; no SDK in v1

The card is a look-at photo ID with no chip. Evolis shows up as a normal Windows printer. v1 opens a credit-card-sized page and uses the browser print dialog. The Evolis developer kit would add encoding and printer control we do not need yet. If the driver is not good enough later, we can add the kit without changing the domain.
